/* let getXAndYCoordsPreOffset = (width, height) =>
     Belt.Array.makeBy(width, x => Belt.Array.makeBy(height, y => (x, y)));

   /* let getXAndYCoords = (x, y, width, height) =>  */

   getXAndYCoordsPreOffset(2, 3)->Js.log; */
